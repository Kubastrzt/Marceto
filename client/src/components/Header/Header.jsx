import React from 'react';
import Navigation from "@/components/Navigation/Navigation";

const Header = ()=>{
    return(
        <header>
            Header
            <Navigation/>
        </header>
    );
}

export default Header